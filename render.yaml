services:
  - type: web
    name: transittrack-backend
    env: docker
    rootDir: transittrack-backend
    dockerfilePath: Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: SPRING_PROFILES_ACTIVE
        value: dev

  - type: web
    name: bus-tracker-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        value: mongodb+srv://your-username:your-password@cluster.mongodb.net/bus-tracker
      - key: JWT_SECRET
        value: your-super-secret-jwt-key-for-production
      - key: ALLOWED_ORIGINS
        value: https://realtime-bus-tracker.netlify.app
